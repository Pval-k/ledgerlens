# Hoist Nest packages to the workspace root so TypeScript/IDEs resolve
# `@nestjs/*` imports when the workspace folder is the monorepo root (pnpm
# otherwise keeps them only under apps/api/node_modules).
public-hoist-pattern[]=@nestjs/*
