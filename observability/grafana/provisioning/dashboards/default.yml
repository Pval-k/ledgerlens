apiVersion: 1

providers:
  - name: LedgerLens
    orgId: 1
    folder: LedgerLens
    type: file
    disableDeletion: false
    updateIntervalSeconds: 30
    options:
      path: /var/lib/grafana/dashboards
