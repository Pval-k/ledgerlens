# Optional. Leave unset during `pnpm dev` to use the Vite proxy (/api → port 3000).
# For `vite build` / production, set the public API origin (no trailing slash).
# VITE_API_URL=https://api.example.com
