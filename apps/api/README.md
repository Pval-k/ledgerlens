# API (`apps/api`)

This will be the **NestJS REST API**.

Planned responsibilities:
- authentication + authorization (JWT)
- document lifecycle endpoints (create/status/retry)
- orchestration: enqueue ingestion jobs to the worker
- query APIs: transactions (pagination/filtering), analytics summaries
- OpenAPI/Swagger docs

Not implemented yet (Stage 0 scaffolding only).

